# FaceClass - Minimal Dependencies (Guaranteed No Conflicts)
# Only essential packages that work together without issues

# Core scientific computing
numpy>=1.21.0
opencv-python>=4.5.0

# PyTorch (no conflicts)
torch>=1.9.0
torchvision>=0.10.0

# YOLO for detection
ultralytics>=8.0.0

# Basic utilities
pyyaml>=5.4.0
